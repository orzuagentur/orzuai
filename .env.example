# OrzuAI Environment Variables
# Copy this file to .env.local and fill in your values.
# Never commit .env.local or real secrets to version control.

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------

# Public base URL of the application (used for auth redirects and email links)
NEXT_PUBLIC_APP_URL=https://orzux.com

# Public base URL of the admin panel (apps/admin — invite emails and login links)
NEXT_PUBLIC_ADMIN_APP_URL=https://admin.orzux.com

# Optional: Vercel env sync in Admin → API ключи → «Синхронизация с Vercel»
# Admin (orzuai-admin) reads secrets from the MAIN web project orzuaibot (orzux.com),
# NOT from the admin project's own env. Required on the admin Vercel project:
# VERCEL_ACCESS_TOKEN=your-vercel-token
# VERCEL_SYNC_PROJECT_ID=prj_1pZTdfXPPP2hiY4uGAWjBtqwLzBa
# VERCEL_SYNC_PROJECT_NAME=orzuaibot
# VERCEL_TEAM_ID=team_rRA61vEP6JGZ9Ezty2ElzGy8

# -----------------------------------------------------------------------------
# Supabase
# -----------------------------------------------------------------------------

# Supabase project URL (Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anon/public key (Project Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase service role key — server-side only, never expose to the client
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# AES-256 key for encrypting app_secrets in Supabase (32+ chars or 64 hex)
# Generate (macOS/Linux): openssl rand -hex 32
# Generate (Windows/Node): node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-64-char-hex-encryption-key

# -----------------------------------------------------------------------------
# Resend (Email)
# -----------------------------------------------------------------------------

# Resend API key (https://resend.com/api-keys)
RESEND_API_KEY=re_your_resend_api_key

# Verified sender address in Resend (bare email or "OrzuX <support@orzux.com>")
RESEND_FROM_EMAIL=support@orzux.com

# -----------------------------------------------------------------------------
# Gemini (AI)
# -----------------------------------------------------------------------------

# Google Gemini API key (https://aistudio.google.com/apikey)
GEMINI_API_KEY=your-gemini-api-key

# Optional: override default Gemini model
# Override on Vercel/local if needed (must match a valid Gemini API model id)
GEMINI_DEFAULT_MODEL=gemini-2.5-flash

# -----------------------------------------------------------------------------
# OpenAI (optional multi-model provider)
# -----------------------------------------------------------------------------

OPENAI_API_KEY=sk-your-openai-api-key

# -----------------------------------------------------------------------------
# ElevenLabs (AI voice replies in chat — Agent → Voice Agent)
# -----------------------------------------------------------------------------

# ELEVENLABS_API_KEY=

# -----------------------------------------------------------------------------
# Anthropic Claude (optional multi-model provider)
# -----------------------------------------------------------------------------

ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# -----------------------------------------------------------------------------
# Google OAuth (Supabase Auth provider)
# Required for Google Login — configure in Supabase Dashboard or local Supabase
# Also used for Google Calendar integration (separate OAuth flow with Calendar scopes)
# and Gmail integration (Gmail API scopes) — add both redirect URIs:
#   https://orzux.com/api/integrations/google-calendar/callback
#   https://orzux.com/api/integrations/gmail/callback
# Enable Google Calendar API, Gmail API, and (for instant sync) Cloud Pub/Sub API
# in the same Google Cloud project. Pub/Sub setup: docs/gmail-pubsub-setup.md
# -----------------------------------------------------------------------------

GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Gmail instant sync (Google Cloud Pub/Sub push — optional but recommended)
# Setup guide: docs/gmail-pubsub-setup.md
# 1) Enable Cloud Pub/Sub API in the SAME Google Cloud project as OAuth
# 2) Create topic (e.g. gmail-inbox) and grant gmail-api-push@system.gserviceaccount.com Publisher
# 3) Create push subscription → https://orzux.com/api/webhooks/gmail-push?token=YOUR_SECRET
# 4) Set env vars below and redeploy; reconnect Gmail or wait for daily watch renew cron
GMAIL_PUBSUB_TOPIC=projects/your-gcp-project-id/topics/gmail-inbox
GMAIL_PUBSUB_PUSH_SECRET=generate-a-long-random-secret

# -----------------------------------------------------------------------------
# Microsoft Outlook / Microsoft 365 (Microsoft Graph OAuth)
# Azure Portal → App registrations → New registration
# Redirect URI (Web): https://orzux.com/api/integrations/outlook/callback
# (must match NEXT_PUBLIC_APP_URL exactly — use www if that is your app origin)
# API permissions (Delegated): offline_access, User.Read, Mail.Read, Mail.Send
# Supported account types: personal Microsoft + work/school → tenant "common"
# Connect UI: /dashboard/integrations/email
# Cron poll: /api/cron/outlook-sync (every 3 minutes via vercel.json)
# -----------------------------------------------------------------------------

MICROSOFT_CLIENT_ID=your-azure-app-client-id
MICROSOFT_CLIENT_SECRET=your-azure-app-client-secret
# Optional. Default "common" allows personal Outlook + Microsoft 365.
# MICROSOFT_TENANT_ID=common

# -----------------------------------------------------------------------------
# WhatsApp Business API via 360dialog
# Inbound webhook (per number): https://orzux.com/api/webhooks/whatsapp
# Partner webhook (onboarding events): https://orzux.com/api/webhooks/360dialog-partner
# -----------------------------------------------------------------------------
#
# Embedded Signup (Integrated Onboarding) — connect a number in ~1 minute from Integrations
# Partner Hub: set redirect URL to https://orzux.com/dashboard/integrations/whatsapp
# and partner webhook to https://orzux.com/api/webhooks/360dialog-partner
NEXT_PUBLIC_DIALOG360_PARTNER_ID=
DIALOG360_PARTNER_API_KEY=
# Optional: verify x-360dialog-signature on partner webhooks (recommended in production)
# DIALOG360_PLATFORM_SECRET=
#
# Manual fallback: each business can paste Number API key + Phone number ID in Integrations
# Get credentials from https://hub.360dialog.com
#
# Sandbox (dev/testing only) — uses waba-sandbox.360dialog.io instead of production API
# DIALOG360_USE_SANDBOX=true
# Or override the API host explicitly:
# DIALOG360_API_BASE=https://waba-sandbox.360dialog.io
#
# Optional: only if you validate Meta-style webhook signatures (usually not needed for 360dialog)
# WHATSAPP_APP_SECRET=

# -----------------------------------------------------------------------------
# WhatsApp Web via QR (Baileys worker)
# Main app uses WORKER_URL + SECRET to send replies through the live worker.
# Deploy apps/whatsapp-web with the worker-only values below.
# -----------------------------------------------------------------------------

WHATSAPP_WEB_WORKER_URL=https://your-whatsapp-web-worker.example.com
WHATSAPP_WEB_SECRET=generate-a-long-random-shared-secret

# Worker-only env:
# SUPABASE_URL=https://your-project-id.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
# ENCRYPTION_KEY=your-64-char-hex-encryption-key
# WHATSAPP_WEB_INGEST_URL=https://orzux.com/api/internal/whatsapp-web/ingest
# WHATSAPP_WEB_STATUS_URL=https://orzux.com/api/internal/whatsapp-web/status
# WHATSAPP_WEB_REFRESH_MS=15000

# -----------------------------------------------------------------------------
# Website Forms — inbound leads from any CMS
# Webhook URL is generated per business after enabling in Integrations → Website Forms
# Example: https://orzux.com/api/webhooks/website-forms/{webhook_token}
# Optional header for server-side POST: X-OrzuAI-Api-Key: {api_key_from_dashboard}
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Website Knowledge Sync — crawl site → knowledge_base for all AI channels
# Cron (Vercel): GET /api/cron/sync-website-knowledge with Authorization: Bearer {CRON_SECRET}
# -----------------------------------------------------------------------------

CRON_SECRET=your-random-cron-secret-min-16-chars

# -----------------------------------------------------------------------------
# Messaging scale (Phase 3–4) — recommended for production inbox
# -----------------------------------------------------------------------------
# Enable Realtime replication in Supabase Dashboard for: messages, conversations,
# message_deliveries (required for live inbox).
#
# Optional CDN origin for chat media signed URLs (e.g. Supabase custom domain)
# MEDIA_CDN_URL=https://cdn.yourdomain.com
#
# Upstash Redis — hot cache for signed media URLs (falls back to Postgres)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
#
# QStash — durable webhook/delivery/hydration workers (primary queue drain)
# Create at https://console.upstash.com/qstash
# Worker URLs (base: https://orzux.com): /api/workers/webhook-queue, /api/workers/inbound-media-hydration,
# /api/workers/outbound-thumbnail, /api/workers/message-deliveries
QSTASH_TOKEN=
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=
#
# Parallel jobs per worker invocation (default: 5, max: 25)
# WORKER_CONCURRENCY=5
#
# Cron fallback (Bearer CRON_SECRET): /api/cron/webhook-queue, /api/cron/message-deliveries,
# /api/cron/ai-health (drains ai_reply_jobs + ai_orchestration_jobs)
# /api/cron/gmail-sync (polls connected Gmail inboxes — recommended every 2–5 min)
# Health + drain: GET /api/cron/messaging-health (every 2 min recommended)
# See README.md → Operations runbook for backlog / hydration troubleshooting.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Telegram Bot API — Phase 8.7
# Requires HTTPS NEXT_PUBLIC_APP_URL (same as production app URL)
# Webhook URL registered automatically per bot: https://orzux.com/api/webhooks/telegram
# Per-bot webhook secrets are stored in telegram_connections (no env var required)
# -----------------------------------------------------------------------------

# Optional legacy global secret (unused when connecting via Integrations UI)
TELEGRAM_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Stripe Billing (subscription checkout + Customer Portal)
# Webhook URL (must use www — orzux.com redirects with 308 and Stripe fails):
#   https://www.orzux.com/api/webhooks/stripe
# Events: checkout.session.completed, customer.created, customer.subscription.*,
#   invoice.paid, invoice.payment_failed, invoice.upcoming, invoice.finalized,
#   customer.source.expiring
# Bootstrap: node --env-file=.env.stripe-setup scripts/setup-stripe-billing.mjs
# Enable Customer Portal in Stripe Dashboard → Settings → Billing → Customer portal
# Disable duplicate Stripe customer emails after OrzuX billing emails are live.
# -----------------------------------------------------------------------------

STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret

# Production price IDs (USD/month). Re-sync via scripts/setup-stripe-billing.mjs
STRIPE_PRICE_STARTER=price_1TpEPBFr2dKz1Bb0vF0lCoQb
STRIPE_PRICE_PRO=price_1TpEWzFr2dKz1Bb0U9lAkkoN
STRIPE_PRICE_AGENCY=price_1TpxVyFr2dKz1Bb0sM64vkkJ
STRIPE_PRICE_ADDON_AI_REPLIES=price_1TpEX3Fr2dKz1Bb0PB5bIcc4
STRIPE_PRICE_ADDON_VOICE_MINUTES=price_1TpEaEFr2dKz1Bb06hGsdskf
STRIPE_PRICE_ADDON_TEAM_SEAT=price_1TpEaGFr2dKz1Bb0iEELCRDj
# Owner internal test pack ($5/mo) — keep in Stripe, not used by public checkout
# STRIPE_TEST_PACK_PRICE=price_1TpxVvFr2dKz1Bb0kDSKlzJm
# Stripe Product ID for monthly Twilio phone number line items
STRIPE_PRODUCT_TWILIO_NUMBER=prod_UpyUe7Jnywn9q2

# -----------------------------------------------------------------------------
# Web Push (new-lead notifications when user is away from the site)
# Generate keys: npx web-push generate-vapid-keys
# -----------------------------------------------------------------------------

NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
# mailto: or https: contact for the push application server
VAPID_SUBJECT=mailto:support@orzux.com

# -----------------------------------------------------------------------------
# AI Voice Agent (outbound callback after order + inbound AI answers)
# Configure per business in Dashboard → Integrations → AI Voice
# Cron: GET /api/cron/voice-callbacks with Authorization: Bearer {CRON_SECRET}
# -----------------------------------------------------------------------------

# Twilio — OrzuX platform account (owns all customer DIDs; HeyKiki-style)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token
# Optional dedicated HMAC secret for Orzu-signed voice webhooks.
# Set PREVIOUS during rotation, then remove it after all old Twilio webhook URLs are refreshed.
TWILIO_WEBHOOK_SIGNING_SECRET=generate-a-long-random-secret
# TWILIO_WEBHOOK_SIGNING_SECRET_PREVIOUS=
# Processing fee % added to Twilio wallet top-ups (covers Stripe fees). Default: 5
TWILIO_TOPUP_PROCESSING_FEE_PERCENT=5

# Inbound webhook on Twilio number:
# https://orzux.com/api/webhooks/voice/inbound?businessId={business_uuid}
# Outbound TwiML URL:
# https://orzux.com/api/webhooks/voice/outbound?businessId={business_uuid}

# Optional: Retell AI or Vapi (set provider in dashboard voice settings)
RETELL_API_KEY=
VAPI_API_KEY=

# Real-time AI phone streaming (Twilio Media Streams + Deepgram + ElevenLabs)
# Deploy apps/voice-stream on Railway/Fly/Render with public wss URL.
# Keys are resolved from Admin → General API AI (env vars below are optional fallback).
# VOICE_STREAM_WS_URL=wss://voice.your-domain.com/voice/stream
# Live monitor supervisors connect to /voice/monitor on the same host (no extra service).
# VOICE_STREAM_SECRET=generate-a-long-random-secret
# ELEVENLABS_API_KEY=
# DEEPGRAM_API_KEY=
# Optional Deepgram tuning (voice-stream worker on Railway)
# DEEPGRAM_ENDPOINTING_MS=600
# DEEPGRAM_UTTERANCE_END_MS=1500
# Optional: override realtime voice LLM (defaults to gpt-4o-mini when OpenAI is configured)
# VOICE_STREAM_LLM_PROVIDER=openai
# VOICE_STREAM_LLM_MODEL=gpt-4o-mini
# VOICE_STREAM_PORT=8081

# -----------------------------------------------------------------------------
# Load tests (scripts/load-tests — optional, not required for app runtime)
# -----------------------------------------------------------------------------

# Inbox concurrent sessions: npm run load-test:inbox
# LOAD_TEST_EMAIL=
# LOAD_TEST_PASSWORD=
# LOAD_TEST_BUSINESS_ID=

# Webhook throughput: npm run load-test:webhooks
# LOAD_TEST_WEBHOOK_COUNT=1000
# LOAD_TEST_WEBHOOK_DURATION_SEC=60
# LOAD_TEST_WEBHOOK_CHANNEL=whatsapp
# LOAD_TEST_INBOX_SESSIONS=100
# LOAD_TEST_INBOX_ROUNDS=3
